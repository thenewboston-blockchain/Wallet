export enum ButtonColor {
  primary = 'primary',
  secondary = 'secondary',
  danger = 'danger',
}

export enum ButtonSize {
  regular = 'regular',
  small = 'small',
}

export enum ButtonType {
  button = 'button',
  reset = 'reset',
  submit = 'submit',
}

export enum ButtonVariant {
  contained = 'contained',
  link = 'link',
  outlined = 'outlined',
}
