export enum ButtonColor {
  primary = 'primary',
}

export enum ButtonType {
  button = 'button',
  reset = 'reset',
  submit = 'submit',
}

export enum ButtonVariant {
  contained = 'contained',
  outlined = 'outlined',
}
