import React from 'react';

import {SFC} from '@shared/types';
import * as S from './Styles';

const NodeConfirmations: SFC = ({className}) => {
  return <S.Container className={className}>NodeConfirmations</S.Container>;
};

export default NodeConfirmations;
