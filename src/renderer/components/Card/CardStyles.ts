import styled from 'styled-components';
import colors from '@renderer/styles/colors';

export const Card = styled.div`
  background: ${colors.white};
  border-radius: 8px;
  box-shadow: 0 1px 6px rgba(9, 51, 83, 0.1);
`;