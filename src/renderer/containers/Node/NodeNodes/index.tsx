import {SFC} from 'shared/types';
import * as S from './Styles';

const NodeNodes: SFC = ({className}) => {
  return <S.Container className={className}>NodeNodes</S.Container>;
};

export default NodeNodes;
