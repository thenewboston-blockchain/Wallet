import React from 'react';

import {SFC} from '@renderer/types';
import * as S from './Styles';

const NodeAccounts: SFC = ({className}) => {
  return <S.Container className={className}>NodeAccounts</S.Container>;
};

export default NodeAccounts;
