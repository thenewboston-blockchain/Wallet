import styled from 'styled-components';
import UFormControlLabel from '@mui/material/FormControlLabel';
import USwitch from '@mui/material/Switch';

export const FormControlLabel = styled(UFormControlLabel)`
  & {
    padding-left: 12px;
  }
`;

export const Switch = styled(USwitch)``;
