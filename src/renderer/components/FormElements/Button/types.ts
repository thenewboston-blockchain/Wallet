export enum ButtonColor {
  primary = 'primary',
  secondary = 'secondary',
}

export enum ButtonType {
  button = 'button',
  reset = 'reset',
  submit = 'submit',
}

export enum ButtonVariant {
  contained = 'contained',
  outlined = 'outlined',
}
