import {SFC} from 'shared/types';
import * as S from './Styles';

const NodeBlocks: SFC = ({className}) => {
  return <S.Container className={className}>NodeBlocks</S.Container>;
};

export default NodeBlocks;
